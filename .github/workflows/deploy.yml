name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Run Bash Script
        run: |
          docker run --rm ubuntu:latest /bin/bash -c "echo 'Ruslan' && echo 'Hurskyi'"

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: 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 
          publish_dir: master
