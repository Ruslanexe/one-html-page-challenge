name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Run Bash Script
        run: |
          docker run -it --rm ubuntu:latest /bin/bash -c "echo 'Ruslan' && echo 'Hurskyi'"

